<main>
    <h1>Home</h1>

{{#each books}}
<div class="books">
    <h3>Titulo: {{this.name}}</h3>
    <p>Quatidade de páginas: {{this.pageqty}}</p>
   
   <div class="button">
        <a href="/book/{{this.id}}" class="mais">Ver mais</a>
        <a href="/edit/{{this.id}}" class="mais">Editar</a>
    </div>
</div>
{{/each}}
</main>